packages:
  - admin
overrides:
  # Axe's auto-installed peer must match the screenshot runner's Page types.
  playwright-core: 1.63.0
allowBuilds:
  esbuild: true
  sharp: true
  workerd: true
minimumReleaseAge: 2880
